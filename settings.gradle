rootProject.name = 'gradle-dependency-resolution-issue-example'

dependencyResolutionManagement {
    repositories {
        mavenCentral()
    }
}

include(":service-a:impl:backend")
include(":service-b:impl:backend")
include(":service-c:impl:backend")
include(":tests")
